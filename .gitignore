# scratch / regenerable data — never commit
*.dat
*.log
*.pfw
*.tar
*.qcow2
__pycache__/
*.pyc

# local tooling / editor / agent config
.claude/
.wakatime-project

# stray empty capture
results/**/*_gdsstats.txt

# python envs (the real env is /opt/gds-venv on the image)
venv/
.venv/
